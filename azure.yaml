name: tdd-azd-avd
metadata:
  template: tdd-azd-avd@0.0.1-beta
infra:
   provider: "bicep"

hooks:
  postprovision:
    windows:
      shell: pwsh
      run: ./scripts/post-provision.ps1
    posix:
      shell: sh
      run: ./scripts/post-provision.sh




      