name: tdd-azd-avd
metadata:
  template: tdd-azd-avd@0.0.1-beta

infra:
  provider: "bicep"

hooks:
  postdeploy:
    windows:
      shell: pwsh
      run: .azd\hooks\postdeploy.ps1
    posix:
      shell: sh  
      run: |
        pwsh .azd/hooks/postdeploy.ps1





      